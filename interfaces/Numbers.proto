syntax = "proto3";

option go_package = "protobuf";
package primefactor;

service Echo {
    rpc EchoNumbers (stream Request) returns (stream Response) {}
}

message Request {
    int64 num = 1;
}

message Response {
    int64 result = 1;
}
